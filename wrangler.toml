name = "maid-chart-downloader"
main = "index.html"
compatibility_date = "2024-01-01"

# Cloudflare Pages 配置
[site]
bucket = "."

# 排除不需要的文件
[build]
command = ""

# 路由规则
[[routes]]
pattern = "/*"
zone_name = ""
